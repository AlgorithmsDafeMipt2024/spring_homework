Rehash
Для закрытого хэширования - a >= 2/3
Для открытого хэширования - a >= 19/20

Для нас - std::hash

h(k) = h'(k) % n
h(k, i) = (h1'(k) + i*h2'(k))%n - двойное хэширование

std::unordered_map, std::unordered_set (std::multimap, std::multiset) - стандартные хэш-таблицы библиотеки

